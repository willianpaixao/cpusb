 x 8 Include libudev library for automatic detection od removible devices
        - Instead, I use inotify_event library.

 x 6 Turn it a daemon
        - I use, as expected, daemon () system call.

 o 6 Build .configure

 x 4 Make a better Makefile
        - It's good enough.

 o 3 Write README e INSTALL

 o 3 Include default values for paths and configuration

 o 3 Improve the report system. Their functions have missing features.


